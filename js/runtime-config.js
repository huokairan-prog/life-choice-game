/*
 * Public, non-secret runtime settings.
 *
 * Set LIFE_GROWTH_API_BASE to the HTTPS API domain after the payment backend is
 * deployed, for example: "https://api.example.com". Never put merchant keys,
 * private keys, APIv3 keys, or OAuth secrets in this file.
 */
window.LIFE_GROWTH_API_BASE = window.LIFE_GROWTH_API_BASE || '';
